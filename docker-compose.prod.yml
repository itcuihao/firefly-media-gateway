services:
  media-gateway:
    image: firefly-media-gateway:latest
    container_name: firefly-media-gateway
    env_file:
      - .env
    ports:
      - "${HOST_PORT:-9090}:8080"
    volumes:
      - mg-data:/data
    restart: unless-stopped

volumes:
  mg-data:
